import { AppunlessDirective } from './appunless.directive';

describe('AppunlessDirective', () => {
  it('should create an instance', () => {
    const directive = new AppunlessDirective();
    expect(directive).toBeTruthy();
  });
});
